import * as THREE from "three";

export function FPMark() {
  return (
    <group position={[-2, -0.79, 0]} rotation={[-Math.PI / 2, 0, 0]}>
      {/* Placeholder untuk Path/Model huruf FP yang sebenarnya diukir. */}
      {/* Anda bisa memuat useGLTF disini nantinya */}
    </group>
  );
}